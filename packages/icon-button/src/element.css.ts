import { css } from 'lit';

export const styles = css`
  :host {
    display: inline-flex;
    height: 1.5em;
    width: 1.5em;
  }
  svg {
    height: 100%;
    width: auto;
    fill: currentColor;
  }
`;

export default styles;
