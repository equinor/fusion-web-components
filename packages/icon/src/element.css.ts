import { css } from 'lit';

export const style = css`
  :host {
    display: inline-flex;
    height: 1.5em;
    width: 1.5em;
  }
  svg {
    height: auto;
    width: auto;
    fill: currentColor;
  }
`;

export default style;
