import { Meta, Story, Canvas, Description } from '@storybook/addon-docs';
import { CustomElement, PackageInfo } from './Components';

import '@equinor/fusion-wc-textarea';

export const Container = ({children}) => (
  <div style={{display: 'flex', flexFlow: 'column', gap: '10px'}}>
    {children}
  </div>
);

<Meta title="Input/TextArea" />

<PackageInfo pkg={require('@equinor/fusion-wc-textarea/package.json')}/>

<CustomElement tag="fwc-textarea" data={require('@equinor/fusion-wc-textarea/lib/custom-elements.json')} />

## Stories

<Story name="General">
  <Container>
    <fwc-textarea cols={50} rows={5} label="text area"/>
    <fwc-textarea label="has error" errorMessage="This is a static error message"/>
    <fwc-textarea label="is disabled" disabled/>
  </Container>
</Story>

