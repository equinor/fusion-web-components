import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { iconNames } from '@equinor/fusion-wc-icon';

import Avatar, { AvatarWrapper } from './Components/Avatar';

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '10px' }}>{children}</div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="Sizes">
      <fwc-avatar size="x-small" {...props} />
      <fwc-avatar size="small" {...props} />
      <fwc-avatar size="medium" {...props} />
      <fwc-avatar size="large" {...props} />
    </Container>
    <Container title="Colors">
      <fwc-avatar color="primary" {...props} />
      <fwc-avatar color="secondary" {...props} />
      <fwc-avatar color="success" {...props} />
      <fwc-avatar color="danger" {...props} />
      <fwc-avatar color="warning" {...props} />
      <fwc-avatar color="disabled" {...props} />
    </Container>
    <Container title="Custom Colors">
      <fwc-avatar style={{ '--fwc-avatar-color': '#495057' }} {...props} />
      <fwc-avatar style={{ '--fwc-avatar-color': '#ffc6ff', '--fwc-avatar-font-color': '#000' }} {...props} />
      <fwc-avatar style={{ '--fwc-avatar-color': '#81b29a', '--fwc-avatar-font-color': '#335c67' }} {...props} />
    </Container>
    <Container title="Badge">
      <fwc-avatar size="x-small" {...props}>
        <fwc-badge slot="badge" size="x-small" position="bottom-right" color="success" circular />
      </fwc-avatar>
      <fwc-avatar size="small" {...props}>
        <fwc-badge slot="badge" size="small" position="bottom-right" color="success" circular />
      </fwc-avatar>
      <fwc-avatar size="medium" {...props}>
        <fwc-badge slot="badge" size="medium" position="bottom-right" color="success" circular />
      </fwc-avatar>
      <fwc-avatar size="large" {...props}>
        <fwc-badge slot="badge" size="large" position="bottom-right" color="success" circular />
      </fwc-avatar>
    </Container>
    <Container title="Clickable">
      <fwc-avatar clickable {...props} />
    </Container>
    <Container title="Disabled">
      <fwc-avatar clickable disabled {...props} />
    </Container>
  </div>
);

# Avatar

<Meta
  title="Data/Avatar"
  component={Avatar}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

```sh
npm i @equinor/fusion-wc-avatar
```

<Canvas>
  <Story name="Default" args={{ label: 'avatar' }} parameters={{ chromatic: { disableSnapshot: true } }}>
    {Avatar.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
