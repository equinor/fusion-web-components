import { html } from 'lit-element';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { AvatarElement } from '@equinor/fusion-wc-avatar';
AvatarElement;

<Meta
  title="Data/avatar"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return html`<fwc-avatar
    src="https://www.w3schools.com/howto/img_avatar.png"
    position="X-External"
    presence="Available"
    size="medium"
    clickable
  />`;
};

export const Initial = ({}) => {
  return html`<fwc-avatar initial="A" size="medium" position="Employee" presence="Away" clickable />`;
};

## Avatar

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Initial

<Canvas>
  <Story name="initial" args={{}}>
    {Initial.bind({})}
  </Story>
</Canvas>
