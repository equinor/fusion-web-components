import { html } from 'lit-element';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { BadgeElement } from '@equinor/fusion-wc-badge';
BadgeElement;

<Meta
  title="Data/badge"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return html`<div style="width: 3.5em; height: 3.5em; position: relative; background-color: gray">
    <fwc-badge size="medium" icon="settings"></fwc-badge>
  </div>`;
};

export const Circular = ({}) => {
  return html`<div style="width: 3.5em; height: 3.5em; position: relative; background-color: gray; border-radius: 50%">
    <fwc-badge size="medium" circular icon="settings"></fwc-badge>
  </div>`;
};

## Badge

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
  <Story name="circular" args={{}}>
    {Circular.bind({})}
  </Story>
</Canvas>
