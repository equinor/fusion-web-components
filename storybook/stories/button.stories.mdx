import { html } from 'lit-element';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Element from '@equinor/fusion-wc-button';
Element;

<Meta
  title="Basic/button"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return html`<fwc-button>Test</fwc-button>`;
};

## Checkbox

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>
