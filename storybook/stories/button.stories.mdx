import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { iconNames } from '@equinor/fusion-wc-icon';

import Button from './Components/Button';

export const Container = ({ title, children}) => (
  <section >
    <h4>{title}</h4>
    <div style={{display: 'flex', gap: '10px'}}> 
      {children}
    </div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="standard">
      <fwc-button label="Primary" color="primary" {...props} />
      <fwc-button label="Secondary" color="secondary" {...props} />
      <fwc-button label="Danger" color="danger" {...props}/>
      <fwc-button label="Disabled" {...props} disabled />
    </Container>
    <Container title="dense">
      <fwc-button label="Primary" color="primary" {...props} dense />
      <fwc-button label="Secondary" color="secondary" {...props} dense />
      <fwc-button label="Danger" color="danger" {...props} dense />
      <fwc-button label="Disabled" {...props} disabled dense />
    </Container>
  </div>
);

# Button
<Meta
  title="Input/Button"
  component={Button}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
  argTypes={{
    icon: { 
      control: 'select',
      options: iconNames,
      },
  }}
/>

```sh
npm i @equinor/fusion-wc-button
```

<Canvas>
  <Story 
    name="Basic" 
    args={{label: 'button'}}
    parameters={{chromatic: { disableSnapshot: true }}}
  >
    {Button.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

## Basic

### Contained
<Story name="Contained" args={{variant: 'contained'}}>
  { Template.bind({}) }
</Story>

### Outlined
<Story name="Outlined" args={{variant: 'outlined'}}>
  { Template.bind({}) }
</Story>

### Ghost
<Story name="Ghost" args={{variant: 'ghost'}}>
  { Template.bind({}) }
</Story>

## Icon
see full list of icon names in `@equinor/fusion-wc-icon`
### Contained
<Story name="Icon Contained" args={{variant: 'contained', icon:'settings'}}>
  { Template.bind({}) }
</Story>

### Contained Trailing
<Story name="Icon Contained Trailing" args={{variant: 'contained', icon:'settings', trailingIcon: true}}>
  { Template.bind({}) }
</Story>

### Outlined
<Story name="Icon Outlined" args={{variant: 'outlined', icon:'settings'}}>
  { Template.bind({}) }
</Story>

### Outlined Trailing
<Story name="Icon Outlined Trailing" args={{variant: 'outlined', icon:'settings', trailingIcon: true}}>
  { Template.bind({}) }
</Story>

### Ghost
<Story name="Icon Ghost" args={{variant: 'ghost', icon:'settings'}}>
  { Template.bind({}) }
</Story>

### Ghost Traling
<Story name="Icon Ghost Traling" args={{variant: 'ghost', icon:'settings', trailingIcon: true}}>
  { Template.bind({}) }
</Story>






