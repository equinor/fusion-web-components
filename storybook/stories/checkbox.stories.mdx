import { html } from 'lit-element';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Element from '@equinor/fusion-wc-checkbox';
Element;

<Meta title="Basic/checkbox" />

export const Default = ({}) => {
  return html`<fwc-checkbox></fwc-checkbox>
    <fwc-checkbox checked></fwc-checkbox>
    <fwc-checkbox indeterminate></fwc-checkbox>`;
};

export const Disabled = ({}) => {
  return html`<fwc-checkbox disabled></fwc-checkbox>
    <fwc-checkbox checked disabled/></fwc-checkbox>
    <fwc-checkbox indeterminate disabled></fwc-checkbox>`;
};

With `MDX` we can define a story for `Checkbox` right in the middle of our
Markdown documentation.

## Checkbox

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="disabled" args={{}}>
    {Disabled.bind({})}
  </Story>
</Canvas>
