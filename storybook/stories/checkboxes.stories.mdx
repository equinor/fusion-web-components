import { html } from 'lit-element';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Element from '@equinor/fusion-wc-checkbox';
Element;

<Meta title="Basic/checkbox" />

export const Default = ({}) => {
  return html`<fwc-checkbox></fwc-checkbox>`;
};

export const DefaultChecked = ({}) => {
  return html`<fwc-checkbox checked></fwc-checkbox>`;
};

export const DefaultIndeterminate = ({}) => {
  return html`<fwc-checkbox indeterminate></fwc-checkbox>`;
};

export const Disabled = ({}) => {
  return html`<fwc-checkbox disabled></fwc-checkbox>`;
};

export const DisabledChecked = ({}) => {
  return html`<fwc-checkbox disabled checked></fwc-checkbox>`;
};

export const DisabledIndeterminate = ({}) => {
  return html`<fwc-checkbox disabled indeterminate></fwc-checkbox>`;
};

## Checkbox

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
  <Story name="default-checked" args={{}}>
    {DefaultChecked.bind({})}
  </Story>
  <Story name="default-indeterminate" args={{}}>
    {DefaultIndeterminate.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="disabled" args={{}}>
    {Disabled.bind({})}
  </Story>
  <Story name="disabled-checked" args={{}}>
    {DisabledChecked.bind({})}
  </Story>
  <Story name="disabled-indeterminate" args={{}}>
    {DisabledIndeterminate.bind({})}
  </Story>
</Canvas>
