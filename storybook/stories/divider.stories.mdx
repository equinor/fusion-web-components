import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import Divider from './Components/Divider';

export const Container = ({ title, orientation, children }) => (
  <section>
    <h4>{title}</h4>
    <div
      style={{
        display: 'flex',
        flexDirection: orientation === 'horizontal' ? 'column' : 'row',
        height: orientation === 'vertical' ? '2rem' : 'auto',
        gap: '10px',
      }}
    >
      {children}
    </div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container orientation={props.orientation} title="Color">
      <fwc-divider color="medium" {...props} />
      <fwc-divider color="light" {...props} />
      <fwc-divider color="lighter" {...props} />
    </Container>
    <Container orientation={props.orientation} title="Spacing">
      <fwc-divider spacing="small" {...props} />
      <fwc-divider spacing="medium" {...props} />
      <fwc-divider spacing="large" {...props} />
    </Container>
    <Container orientation={props.orientation} title="Variant">
      <fwc-divider variant="full" {...props} />
      <fwc-divider variant="middle" {...props} />
      <fwc-divider variant="list" {...props} />
    </Container>
  </div>
);

# Divider

<Meta
  title="Layout/Divider"
  component={Divider}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
  argTypes={{}}
/>

```sh
npm i @equinor/fusion-wc-divider
```

## Playground

<Canvas>
  <Story name="Default" args={{}} parameters={{ chromatic: { disableSnapshot: true } }}>
    {Divider.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

### Horizontal

<Story name="Horizontal" args={{ orientation: 'horizontal' }}>
  {Template.bind({})}
</Story>

### Vertical

<Story name="Vertical" args={{ orientation: 'vertical' }}>
  {Template.bind({})}
</Story>
