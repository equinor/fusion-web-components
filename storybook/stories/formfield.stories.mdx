import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import FormField from './Components/FormField';
import extractProps from './Components/extract-props';

import '@equinor/fusion-wc-checkbox';
import '@equinor/fusion-wc-switch';
import '@equinor/fusion-wc-radio';
import '@equinor/fusion-wc-textinput';

export const Container = ({flow, children}) => (
   <div style={{display: 'flex', flexFlow: flow, gap: '10px'}}>
     {children}
   </div>
);

export const Template = (props) => {
  const args = extractProps(props);
  return (
    <Container flow={props.spaceBetween ? 'column': 'row'}>
      <fwc-formfield {...args} >
        <fwc-checkbox />
      </fwc-formfield>
      <fwc-formfield {...args} >
        <fwc-switch />
      </fwc-formfield>
      <fwc-formfield {...args} >
        <fwc-radio />
      </fwc-formfield>
      <fwc-formfield {...args} >
        <fwc-textinput />
      </fwc-formfield>
    </Container>
  );
};

<Meta 
  title="Input/Formfield"
  component={FormField}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }} 
/>

```sh
npm i @equinor/fusion-wc-formfield
```

<Canvas>
  <Story 
    name="Basic" 
    args={{label: 'foobar'}}
    parameters={{chromatic: { disableSnapshot: true }}}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

### Stories

#### default
<Story name="default " args={{label: 'Boudin salami ball'}}>
  {Template.bind({})}
</Story>

#### alignEnd
<Story name="alignment" args={{label: 'Boudin salami ball', alignEnd: true}}>
  {Template.bind({})}
</Story>

#### spaceBetween
<Story name="spacing" args={{label: 'Bacon ipsum', spaceBetween: true, alignEnd: true}}>
  {Template.bind({})}
</Story>

#### nowrap
<Story name="nowrap" args={{spaceBetween: true, alignEnd: true, label: 'Bacon ipsum dolor amet biltong turducken bacon, boudin salami ball tip pork loin ground round rump turkey brisket doner tail jowl drumstick.', nowrap: true}}>
  {Template.bind({})}
</Story>