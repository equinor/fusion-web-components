import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { iconNames } from '@equinor/fusion-wc-icon';

import PersonAvatar, { PersonAvatarWrapper } from './Components/PersonAvatar';
import { Availability, AccountType } from '@equinor/fusion-wc-person';

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '10px' }}>{children}</div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="Sizes">
      <PersonAvatar size="x-small" {...props} />
      <PersonAvatar size="small" {...props} />
      <PersonAvatar size="medium" {...props} />
      <PersonAvatar size="large" {...props} />
    </Container>
    <Container title="Clickable">
      <PersonAvatar clickable {...props} />
    </Container>
    <Container title="Disabled">
      <PersonAvatar disabled {...props} />
    </Container>
  </div>
);

# PersonAvatar

<Meta
  title="People/PersonAvatar"
  component={PersonAvatar}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

```sh
npm i @equinor/fusion-wc-person-avatar
```

## Playground

<Canvas>
  <Story name="Default" args={{ azureId: '1234' }} parameters={{ chromatic: { disableSnapshot: true } }}>
    {PersonAvatar.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

<Story name="Snapshots" args={{}}>
  {Template.bind({})}
</Story>
