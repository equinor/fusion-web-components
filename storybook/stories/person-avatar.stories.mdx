import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { iconNames } from '@equinor/fusion-wc-icon';

import PersonAvatar, { PersonAvatarWrapper } from './Components/PersonAvatar';
import { Availability, AccountType } from '@equinor/fusion-wc-person';

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '10px' }}>{children}</div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="Sizes">
      <fwc-person-avatar size="x-small" {...props} />
      <fwc-person-avatar size="small" {...props} />
      <fwc-person-avatar size="medium" {...props} />
      <fwc-person-avatar size="large" {...props} />
    </Container>
    <Container title="Account Types">
      <fwc-person-avatar accountType={AccountType.Employee} {...props} />
      <fwc-person-avatar accountType={AccountType.ExternalHire} {...props} />
      <fwc-person-avatar accountType={AccountType.XExternal} {...props} />
      <fwc-person-avatar accountType={AccountType.JointVentureAffiliate} {...props} />
    </Container>
    <Container title="Availability">
      <fwc-person-avatar availability={Availability.Available} {...props} />
      <fwc-person-avatar availability={Availability.AvailableIdle} {...props} />
      <fwc-person-avatar availability={Availability.Away} {...props} />
      <fwc-person-avatar availability={Availability.BeRightBack} {...props} />
      <fwc-person-avatar availability={Availability.Busy} {...props} />
      <fwc-person-avatar availability={Availability.BusyIdle} {...props} />
      <fwc-person-avatar availability={Availability.DoNotDisturb} {...props} />
      <fwc-person-avatar availability={Availability.Offline} {...props} />
    </Container>
    <Container title="Clickable">
      <fwc-person-avatar clickable {...props} />
    </Container>
    <Container title="Disabled">
      <fwc-person-avatar disabled {...props} />
    </Container>
  </div>
);

# PersonAvatar

<Meta
  title="Data/PersonAvatar"
  component={PersonAvatar}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

```sh
npm i @equinor/fusion-wc-person-avatar
```

## Playground

<Canvas>
  <Story
    name="Default"
    args={{
      azureId: '1234567890',
    }}
    parameters={{ chromatic: { disableSnapshot: true } }}
  >
    {PersonAvatar.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

### Picture

<Story name="Picture" args={{ pictureSrc: 'https://i.imgur.com/C4iPxwN.jpeg' }}>
  {Template.bind({})}
</Story>

### Initial

<Story name="Initial" args={{ name: 'Ben Dover' }}>
  {Template.bind({})}
</Story>
