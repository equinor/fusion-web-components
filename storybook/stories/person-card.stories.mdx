import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { iconNames } from '@equinor/fusion-wc-icon';

import PersonCard from './Components/PersonCard';
import { Availability, PersonAccountType, PersonItemSize } from '@equinor/fusion-wc-person';

export const DefaultTemplate = ({ children, ...props }) => {
  return (
    <div style={{ margin: '0 auto' }}>
      <PersonCard {...props} />
    </div>
  );
};

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '30px' }}>{children}</div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="Sizes">
      <PersonCard size="small" {...props} />
      <PersonCard size="medium" {...props} />
      <PersonCard size="large" {...props} />
    </Container>
    <Container title="With datasource">
      <PersonCard dataSource={{
        name: 'Tux Penguin (Bouvet ASA)',
        pictureSrc: 'https://i.imgur.com/GcZeeXX.jpeg',
        accountType: PersonAccountType.Consultant,
        jobTitle: 'X-Bouvet ASA (PX)',
        department: 'FOIT CON PDP',
        mail: 'example@email.com',
        officeLocation: 'South Pole',
        mobilePhone: '+47 999999999',
        manager: {
            azureUniqueId: '1234-1324-1235',
            name: 'Lagertha Kristensen',
            department: 'Leader Techn Mgmt',
            pictureSrc:
            'https://cloudflare-ipfs.com/ipfs/Qmd3W5DuhgHirLHGVixi6V76LhCkZUz6pnFt5AJBiyvHye/avatar/814.jpg',
            accountType: PersonAccountType.Employee,
        },
        positions: [
            {
                id: '123-123',
                name: 'Developer Frontend',
                project: {
                    id: '1234-1234',
                    name: 'Fusion',
                },
            }, {
                id: '234-234',
                name: 'Developer Frontend',
                project: {
                    id: '2345-2345',
                    name: 'Fusion org v2',
                },
            },
        ],
      }} />
    </Container>
  </div>
);

# PersonCard

<Meta
  title="People/PersonCard"
  component={PersonCard}
  argTypes={{
    azureId: {
      description: 'Unique person Azure ID',
      type: { name: 'string', required: true },
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: 'medium' },
      },
    },
    size: {
      description: 'Size of the component',
      control: 'radio',
      options: PersonItemSize,
      table: {
        type: { summary: 'PersonItemSize' },
        defaultValue: { summary: 'medium' },
      },
    },
    maxWidth: {
      description: 'Maximum width of the component (minimum 220px)',
      type: { name: 'number' },
      control: { min:220 },
      defaultValue: 300,
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: '300px' },
      },
    },
    contentHeight: {
      description: 'Height of content (px)',
      type: { name: 'number' },
      defaultValue: 150,
      table: {
        type: { summary: 'number' },
        defaultValue: { summary: '150px' },
      },
    },
  }}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

```sh
npm i @equinor/fusion-wc-person
```

## Usage

```html
<fwc-person-provider>
  <fwc-person-card azureId="1234" />
</fwc-person-provider>
```

## Playground

<Canvas>
  <Story
    name="Default"
    args={{ azureId: '1234', size: 'medium' }}
    parameters={{ chromatic: { disableSnapshot: true } }}
  >
    {DefaultTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

<Story name="Snapshots" args={{ azureId: '1234' }}>
  {Template.bind({})}
</Story>
