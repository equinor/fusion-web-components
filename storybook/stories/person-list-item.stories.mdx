import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { iconNames } from '@equinor/fusion-wc-icon';

import PersonListItem from './Components/PersonListItem';
import { Availability, AccountType } from '@equinor/fusion-wc-person';

export const DefaultTemplate = ({ children, ...props }) => {
  return (
    <div style={{ height: '400px' }}>
      <PersonListItem {...props} />
    </div>
  );
};

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '30px', flexDirection: 'column' }}>{children}</div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="Sizes">
      <PersonListItem size="small" {...props} />
      <PersonListItem size="medium" {...props} />
      <PersonListItem size="large" {...props} />
    </Container>
  </div>
);

# PersonListItem

<Meta
  title="People/PersonListItem"
  component={PersonListItem}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

```sh
npm i @equinor/fusion-wc-person
```

## Usage

```html
<fwc-person-provider>
  <fwc-person-list-item azureId="1234" />
</fwc-person-provider>
```

## Playground

<Canvas>
  <Story
    name="Default"
    args={{ azureId: '1234', size: 'medium' }}
    parameters={{ chromatic: { disableSnapshot: true } }}
  >
    {DefaultTemplate.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

<Story name="Snapshots" args={{}}>
  {Template.bind({})}
</Story>
