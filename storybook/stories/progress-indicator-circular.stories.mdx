import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { CircularIndicator } from './Components/ProgressIndicator';

export const Component = (props) => <CircularIndicator {...props} />;

export const Container = ({ children }) => (
  <section>
    <div style={{ display: 'flex', gap: '15px' }}>{children}</div>
  </section>
);

export const Colors = (props) => (
  <Container>
    <div>
      <h5>Primary</h5>
      <CircularIndicator color="primary" />
    </div>
    <div style={{ borderRadius: '3px', backgroundColor: '#7C7C7C', padding: '0 10px 10px' }}>
      <h5 style={{ color: 'white' }}>Neutral</h5>
      <CircularIndicator color="neutral" />
    </div>
  </Container>
);

export const Sizes = (props) => (
  <Container>
    <CircularIndicator size={48} />
    <CircularIndicator size={40} />
    <CircularIndicator size={32} />
    <CircularIndicator size={24} />
    <CircularIndicator size={16} />
  </Container>
);

<Meta
  title="Progress Indicator/Circular"
  component={CircularIndicator}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

<Canvas>
  <Story name="Default" args={{ size: '32', color: 'primary' }}>
    {Component.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

### Colors

<Story name="Colors">{Colors.bind({})}</Story>

### Sizes

<Story name="Sizes">{Sizes.bind({})}</Story>
