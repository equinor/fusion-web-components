import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { DotsIndicator } from './Components/ProgressIndicator';

export const Component = (props) => <DotsIndicator {...props} />;

export const Container = ({ children }) => (
  <section>
    <div style={{ display: 'flex', gap: '2rem' }}>{children}</div>
  </section>
);

export const Colors = (props) => (
  <Container>
    <div>
      <h5>Primary</h5>
      <div style={{ padding: '10px 0' }}>
        <DotsIndicator color="primary" />
      </div>
    </div>
    <div>
      <h5>Tertiary</h5>
      <div style={{ padding: '10px 0' }}>
        <DotsIndicator color="tertiary" />
      </div>
    </div>
    <div>
      <h5>Neutral</h5>
      <div style={{ borderRadius: '3px', backgroundColor: '#DCDCDC', padding: '10px' }}>
        <DotsIndicator color="neutral" />
      </div>
    </div>
  </Container>
);

export const Sizes = (props) => (
  <Container>
    <div>
      <h5>Large</h5>
      <DotsIndicator size="large" />
    </div>
    <div>
      <h5>Medium</h5>
      <DotsIndicator size="medium" />
    </div>
    <div>
      <h5>Small</h5>
      <DotsIndicator size="small" />
    </div>
  </Container>
);

<Meta title="Progress Indicator/Dots" component={DotsIndicator} />

# Dots

Progress indicators are animated helpers that indicate waiting time as content loads.  
Dot progress indicators are for use within buttons.

<Canvas>
  <Story name="Default" args={{ size: 'small', color: 'primary' }}>
    {Component.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Snapshots

### Colors

<Story name="Colors">{Colors.bind({})}</Story>

### Sizes

<Story name="Sizes">{Sizes.bind({})}</Story>
