import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { SkeletonSize } from '@equinor/fusion-wc-skeleton';
import Skeleton from './Components/Skeleton';

export const Container = ({ title, children }) => (
  <section>
    <h4>{title}</h4>
    <div style={{ display: 'flex', gap: '10px' }}>{children}</div>
  </section>
);

export const Template = (props) => (
  <div>
    <Container title="Sizes">
      <fwc-skeleton size="x-small" {...props} />
      <fwc-skeleton size="small" {...props} />
      <fwc-skeleton size="medium" {...props} />
      <fwc-skeleton size="large" {...props} />
    </Container>
    <Container title="Icon">
      <fwc-skeleton size="x-small" {...props}>
        <fwc-icon slot="icon" icon="image"></fwc-icon>
      </fwc-skeleton>
      <fwc-skeleton size="small" {...props}>
        <fwc-icon slot="icon" icon="image"></fwc-icon>
      </fwc-skeleton>
      <fwc-skeleton size="medium" {...props}>
        <fwc-icon slot="icon" icon="image"></fwc-icon>
      </fwc-skeleton>
      <fwc-skeleton size="large" {...props}>
        <fwc-icon slot="icon" icon="image"></fwc-icon>
      </fwc-skeleton>
    </Container>
    <Container title="Inactive">
      <fwc-skeleton size="x-small" inactive {...props} />
      <fwc-skeleton size="small" inactive {...props} />
      <fwc-skeleton size="medium" inactive {...props} />
      <fwc-skeleton size="large" inactive {...props} />
    </Container>
  </div>
);

# Skeleton

<Meta
  title="Data/Skeleton"
  component={Skeleton}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

```sh
npm i @equinor/fusion-wc-skeleton
```

<Canvas>
  <Story name="Default" args={{ label: 'skeleton' }} parameters={{ chromatic: { disableSnapshot: true } }}>
    {Skeleton.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Rectangle

<Story name="Rectangle" args={{ variant: 'rectangle' }}>
  {Template.bind({})}
</Story>

## Square

<Story name="Square" args={{ variant: 'square' }}>
  {Template.bind({})}
</Story>

## Text

<Story name="Text" args={{ variant: 'text' }}>
  {Template.bind({})}
</Story>

## Circle

<Story name="Circle" args={{ variant: 'circle' }}>
  {Template.bind({})}
</Story>
