import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import TextArea from './Components/TextArea';

export const Container = ({children}) => (
  <div style={{display: 'flex', gap: '10px', flexWrap: 'wrap'}}>
    {children}
  </div>
);

<Meta
  title="Input/TextArea"
  component={TextArea}
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>


# TextArea

```sh
npm i @equinor/fusion-wc-textarea
```

<Canvas>
  <Story
    name="Basic"
    args={{ 
      label: 'TextInput',
    }}
    parameters={{chromatic: { disableSnapshot: true }}}
  >
    {TextArea.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Basic" />

## Examples

### General
TODO: outlined does not seem to work
<Story name="General">
  <h4>Filled</h4>
  <Container>
    <fwc-textarea label="default" />
    <fwc-textarea label="readonly" readonly/>
    <fwc-textarea label="disabled" disabled/>
  </Container>
  <h4>Outlined</h4>
  <Container>
    <fwc-textarea label="default" variant="outlined" />
    <fwc-textarea label="readonly" variant="outlined" readonly/>
    <fwc-textarea label="disabled" variant="outlined" disabled/>
  </Container>
</Story>

### Helper text
<Story name="Helper text">
  <Container>
    <fwc-textarea label="Helper text" helper="Helper Text" helperpersistent/>
  </Container>
</Story>

### With Icon
<Story name="With Icon">
  <Container>
    <fwc-textarea label="With Icon" icon="settings"/>
    <fwc-textarea label="With trailing icon" iconTrailing="settings"/>
  </Container>
</Story>

### Character limit
TOOD: missing validation of max length
<Story name="Character limit">
  <Container>
    <fwc-textarea label="Limit" maxlength="18" charCounter value="foo"/>
    <fwc-textarea label="Limit" maxlength="3" charCounter value="foobar"/>
  </Container>
</Story>

### Fluid
TODO: this does not seem to work
<Story name="Fluid">
  <fwc-textarea label="Fluid" fullwidth/>
</Story>

