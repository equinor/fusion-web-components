import { html } from 'lit-element';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Element from '@equinor/fusion-wc-textinput';
Element;

<Meta
  title="Input/textinput"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

export const Default = ({}) => {
  return html`<fwc-textinput label="Default"></fwc-textinput>`;
};

export const Helper = ({}) => {
  return html`<fwc-textinput label="Helper" helper="Helper Text" helperpersistent></fwc-textinput>`;
};

export const Icon = ({}) => {
  return html`<fwc-textinput label="Icon" icon="settings"></fwc-textinput>`;
};

export const TrailingIcon = ({}) => {
  return html`<fwc-textinput label="Icon" iconTrailing="settings"></fwc-textinput>`;
};

export const CharLimit = ({}) => {
  return html`<fwc-textinput label="Limit" maxlength="18" charCounter></fwc-textinput>`;
};

export const FullWidth = ({}) => {
  return html`<fwc-textinput label="Fluid" fullwidth></fwc-textinput>`;
};

export const Disabled = ({}) => {
  return html`<fwc-textinput label="Disabled" disabled></fwc-textinput>`;
};

## TextInput

### Default

<Canvas>
  <Story name="default" args={{}}>
    {Default.bind({})}
  </Story>
</Canvas>

### With helper

<Canvas>
  <Story name="helper" args={{}}>
    {Helper.bind({})}
  </Story>
</Canvas>

### With icon

<Canvas>
  <Story name="icon" args={{}}>
    {Icon.bind({})}
  </Story>
</Canvas>

### With trailing icon

<Canvas>
  <Story name="trailing-icon" args={{}}>
    {TrailingIcon.bind({})}
  </Story>
</Canvas>

### With character limit

<Canvas>
  <Story name="limit" args={{}}>
    {CharLimit.bind({})}
  </Story>
</Canvas>

### Full width (fluid)

<Canvas>
  <Story name="full" args={{}}>
    {FullWidth.bind({})}
  </Story>
</Canvas>

### Disabled

<Canvas>
  <Story name="disabled" args={{}}>
    {Disabled.bind({})}
  </Story>
</Canvas>
